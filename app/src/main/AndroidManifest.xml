<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="winzer.gh0strunner">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <activity
                android:noHistory="true"
                android:screenOrientation="portrait"
                android:name=".MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:noHistory="true"
                android:screenOrientation="portrait"
                android:name=".login.LoginActivity"
                android:label="@string/title_activity_login">
        </activity>
        <activity
                android:noHistory="true"
                android:screenOrientation="portrait"
                android:name=".run.RunActivity"
                android:label="@string/title_activity_run">
        </activity>
        <service
                android:name=".services.RunService"
                android:label="@string/title_service_run">
        </service>
        <service
                android:name=".services.SRTMElevationService"
                android:label="@string/title_service_elevation">
        </service>
    </application>

</manifest>
